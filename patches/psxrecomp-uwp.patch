diff --git a/runtime/src/autocompile.c b/runtime/src/autocompile.c
index a9b2a79..7bb8052 100644
--- a/runtime/src/autocompile.c
+++ b/runtime/src/autocompile.c
@@ -4,6 +4,22 @@
 #include "autocompile.h"
 #include "overlay_loader.h"
 
+#if defined(PSX_UWP)
+#include <stdio.h>
+void autocompile_configure(const char *cmd, const char *cwd) { (void)cmd; (void)cwd; }
+void autocompile_set_cache_paths(const char *cache_dir, const char *captures) { (void)cache_dir; (void)captures; }
+int autocompile_configured(void) { return 0; }
+int autocompile_toolchain_available(void) { return 0; }
+int autocompile_busy(void) { return 0; }
+int autocompile_request(void) { return 0; }
+void autocompile_poll_main(void) {}
+void autocompile_shutdown(void) {}
+int autocompile_status_json(char *out, int cap) {
+    return cap > 0 ? snprintf(out, (size_t)cap, "{\"state\":\"static-uwp\"}") : 0;
+}
+const char *autocompile_degraded_reason(void) { return NULL; }
+#else
+
 #include <stdarg.h>   /* autocompile_set_degraded takes a format + varargs */
 #include <stdint.h>
 #include <stdio.h>
@@ -1232,3 +1248,4 @@ int autocompile_status_json(char *out, int cap) {
         (unsigned long long)publish_prepare_max_us,
         (unsigned long long)publish_prepare_last_us, tail);
 }
+#endif
diff --git a/runtime/src/crash_trace.c b/runtime/src/crash_trace.c
index c9f8b04..82ef498 100644
--- a/runtime/src/crash_trace.c
+++ b/runtime/src/crash_trace.c
@@ -929,7 +929,7 @@ void psx_crash_trace_install_handlers(void) {
     signal(SIGUSR1, psx_soft_exit_handler);
 #endif
     signal(SIGABRT, psx_signal_handler);
-#ifdef _WIN32
+#if defined(_WIN32) && !defined(PSX_UWP)
     /* Let access violations reach the SEH filter with their faulting CONTEXT.
      * MinGW's SIGSEGV bridge discards EXCEPTION_POINTERS, reducing the report
      * to "signal_11" with no native instruction or accessed address. */
diff --git a/runtime/src/freeze_heartbeat.c b/runtime/src/freeze_heartbeat.c
index 46d82b9..2c07e25 100644
--- a/runtime/src/freeze_heartbeat.c
+++ b/runtime/src/freeze_heartbeat.c
@@ -11,9 +11,11 @@
 #include <string.h>
 #include <time.h>
 
-#ifdef _WIN32
+#if defined(_WIN32) && !defined(PSX_UWP)
 #include <windows.h>
 #include <dbghelp.h>
+#elif defined(_WIN32)
+#include <windows.h>
 #endif
 
 /* State accessors. All defined in other compilation units; declared here
@@ -175,7 +177,7 @@ void freeze_heartbeat_set_paused(int paused) {
     s_wedge_classification_paused = paused ? 1 : 0;
 }
 
-#ifdef _WIN32
+#if defined(_WIN32) && !defined(PSX_UWP)
 /* Capture the main thread's call stack at the moment of a hard freeze.
  *
  * Called from the heartbeat thread when wedge_kind==1 (frame_count not
@@ -310,6 +312,14 @@ static void freeze_dump_main_stack_samples_json(FILE *f, int n) {
     }
     fputc(']', f);
 }
+#elif defined(PSX_UWP)
+static void freeze_dump_main_stack_json(FILE *f) {
+    if (f) fputs("[]", f);
+}
+static void freeze_dump_main_stack_samples_json(FILE *f, int n) {
+    (void)n;
+    if (f) fputs("[]", f);
+}
 #endif /* _WIN32 */
 
 /* One dump at a time. The watchdog (heartbeat thread) and psx_fatal_halt
diff --git a/runtime/src/gpu_gl_renderer.c b/runtime/src/gpu_gl_renderer.c
index b85cd55..c1492eb 100644
--- a/runtime/src/gpu_gl_renderer.c
+++ b/runtime/src/gpu_gl_renderer.c
@@ -74,6 +74,9 @@
 #include "latency_ring.h"
 #include "frame_pacing.h"
 #include "psx_rewind.h"
+#if defined(PSX_UWP)
+extern int psx_uwp_settings_overlay_image(const uint32_t **pixels, int *width, int *height);
+#endif
 
 #include "psx_sdl.h"
 #if defined(PSX_SDL3)
@@ -3859,7 +3862,7 @@ void gl_renderer_set_interpolation(int enabled, double host_hz, double target_hz
     double effective_hz = target_hz > 0.0 ? target_hz : host_hz;
     if (effective_hz < source_hz) effective_hz = source_hz;
     int active = (enabled && source_hz >= 1.0 && source_hz <= 1000.0 &&
-                  effective_hz >= source_hz && effective_hz <= 1000.0) ? 1 : 0;
+                  effective_hz > source_hz * 1.05 && effective_hz <= 1000.0) ? 1 : 0;
     const char *diag = getenv("PSX_GL_INTERP_DIAG");
     s_interp_diag = diag && diag[0] && diag[0] != '0';
     if (active != s_interp_enabled || source_hz != s_interp_source_hz ||
@@ -4138,6 +4141,10 @@ static void gl_swap_with_osd(void) {
             }
             if (psx_savestate_menu_overlay_image(&px, &ow, &oh) && px)
                 gl_draw_osd_image(px, ow, oh, ww, wh, 0, 0, ww, wh);
+#if defined(PSX_UWP)
+            if (psx_uwp_settings_overlay_image(&px, &ow, &oh) && px)
+                gl_draw_osd_image(px, ow, oh, ww, wh, 0, 0, ww, wh);
+#endif
         }
     }
     host_osd_present_done();
diff --git a/runtime/src/host_osd.c b/runtime/src/host_osd.c
index 62c387d..8694b00 100644
--- a/runtime/src/host_osd.c
+++ b/runtime/src/host_osd.c
@@ -1,8 +1,3 @@
-/* host_osd.c — top-left toast messages for host hotkeys / savestate results.
- *
- * Visual OSD is gated on RECOMP_LAUNCHER (recomp-ui bundled). Volume state
- * remains available for headless / launcher-less builds. */
-
 #include "host_osd.h"
 #include "psx_rewind.h"
 #include "psx_savestate_menu.h"
@@ -10,6 +5,9 @@
 
 #include <stdio.h>
 #include <string.h>
+#if defined(PSX_UWP)
+extern int psx_uwp_settings_overlay_image(const uint32_t **pixels, int *width, int *height);
+#endif
 
 #if defined(RECOMP_LAUNCHER)
 #define HOST_OSD_VISUAL 1
@@ -169,6 +167,11 @@ static int          s_sdl_rw_th;
 static SDL_Texture *s_sdl_ssm_tex;
 static int          s_sdl_ssm_tw;
 static int          s_sdl_ssm_th;
+#if defined(PSX_UWP)
+static SDL_Texture *s_sdl_uwp_settings_tex;
+static int          s_sdl_uwp_settings_tw;
+static int          s_sdl_uwp_settings_th;
+#endif
 static SDL_Renderer *s_sdl_ren;
 #endif
 
@@ -512,6 +515,15 @@ void host_osd_draw_sdl(struct SDL_Renderer *renderer) {
             sdl_blit_argb(renderer, &s_sdl_ssm_tex, &s_sdl_ssm_tw,
                           &s_sdl_ssm_th, px, w, h, 0, 0, lw, lh);
         }
+#if defined(PSX_UWP)
+        if (psx_uwp_settings_overlay_image(&px, &w, &h) && px) {
+            int lw = 640, lh = 480;
+            sdl_logical_size(renderer, &lw, &lh);
+            sdl_blit_argb(renderer, &s_sdl_uwp_settings_tex,
+                          &s_sdl_uwp_settings_tw, &s_sdl_uwp_settings_th,
+                          px, w, h, 0, 0, lw, lh);
+        }
+#endif
     }
 #else
     (void)renderer;
diff --git a/runtime/src/main.cpp b/runtime/src/main.cpp
index cbf4c83..b1fc190 100644
--- a/runtime/src/main.cpp
+++ b/runtime/src/main.cpp
@@ -87,6 +87,11 @@ extern "C" void psx_event_step_conservative_env_init(void);
 #include "iso_reader.h"      /* text-image guard: extract the boot EXE from the disc */
 #include "psx_keybinds.h"    /* configurable keyboard->DualShock keybinds (keybinds.ini) */
 #include "psx_window_icon.h"
+#if defined(PSX_UWP)
+#include "recomp_runtime_ui.h"
+extern "C" void psx_uwp_save_graphics_settings(int, int, int);
+extern "C" void recomp_runtime_ui_enter_section(RecompRuntimeUi *, size_t);
+#endif
 
 #if defined(RECOMP_LAUNCHER)
 #include "recomp_launcher.h"   /* shared recomp-ui Dear ImGui launcher */
@@ -1260,9 +1265,17 @@ static int           g_video_vsync        = 1;
 static int           g_frame_interpolation = 0;
 static int           g_frame_interpolation_fps = 0;
 static int           g_frame_interpolation_blend =
+#if defined(PSX_UWP)
+    PSX_MOD_FRAME_INTERPOLATION_MOTION_ADAPTIVE;
+#else
     PSX_MOD_FRAME_INTERPOLATION_LINEAR;
+#endif
 static int           g_frame_interpolation_blend_default =
+#if defined(PSX_UWP)
+    PSX_MOD_FRAME_INTERPOLATION_MOTION_ADAPTIVE;
+#else
     PSX_MOD_FRAME_INTERPOLATION_LINEAR;
+#endif
 static std::array<int, PSX_MAX_PLAYERS> g_mod_controller_mode_override =
     [] {
         std::array<int, PSX_MAX_PLAYERS> modes{};
@@ -2130,8 +2143,14 @@ static std::filesystem::path find_upward(std::filesystem::path start,
 // authoritative source (independent of argv[0], which can be relative/bare and
 // would otherwise get resolved against cwd by fs::absolute). $APPIMAGE and
 // argv[0] are non-Windows / fallback sources only.
+#if defined(PSX_UWP)
+extern "C" const char* psx_uwp_data_path(void);
+#endif
 static std::filesystem::path exe_dir_from_argv(const char* argv0) {
     namespace fs = std::filesystem;
+#if defined(PSX_UWP)
+    return fs::u8path(psx_uwp_data_path());
+#endif
     std::error_code ec;
     fs::path exe_dir;
 #ifdef _WIN32
@@ -2205,7 +2224,7 @@ static void write_cached_path(const char* argv0, const char* filename,
 
 static void launcher_warning(const char* title, const std::string& msg) {
     std::fprintf(stderr, "%s: %s\n", title, msg.c_str());
-#ifdef _WIN32
+#if defined(_WIN32) && !defined(PSX_UWP)
     // Headless (--headless / PSX_HEADLESS): NEVER pop a blocking modal — it would
     // hang an unattended/CI/scripted run forever waiting for a click.
     if (!g_headless) MessageBoxA(NULL, msg.c_str(), title, MB_OK | MB_ICONWARNING);
@@ -2214,7 +2233,7 @@ static void launcher_warning(const char* title, const std::string& msg) {
 
 static void launcher_info(const char* title, const std::string& msg) {
     std::fprintf(stderr, "%s: %s\n", title, msg.c_str());
-#ifdef _WIN32
+#if defined(_WIN32) && !defined(PSX_UWP)
     if (!g_headless) MessageBoxA(NULL, msg.c_str(), title, MB_OK | MB_ICONINFORMATION);
 #endif
 }
@@ -2234,7 +2253,7 @@ static bool pick_runtime_file(const char* title, const char* filter,
             title, cli_flag);
         return false;
     }
-#ifdef _WIN32
+#if defined(_WIN32) && !defined(PSX_UWP)
     char path_buf[4096];
     std::memset(path_buf, 0, sizeof(path_buf));
 
@@ -2857,6 +2876,9 @@ static void refresh_host_display_cadence(int force_log, int force_probe) {
             host_hz = (double)dm.refresh_rate;
         }
     }
+#if defined(PSX_UWP)
+    if (host_hz <= 0.0) host_hz = 60.0;
+#endif
 
     const int display_changed = (disp_idx != g_host_refresh_display_idx);
     const int refresh_changed =
@@ -2917,6 +2939,15 @@ static int host_driver_vsync_unreliable(void) {
 #endif
 }
 
+static int frame_interpolation_has_refresh_headroom(void) {
+    if (!g_frame_interpolation) return 0;
+    const double source_hz = g_frame_period_ms > 0.0
+        ? 1000.0 / g_frame_period_ms : 59.94;
+    const double target_hz = g_frame_interpolation_fps > 0
+        ? (double)g_frame_interpolation_fps : g_host_refresh_hz;
+    return target_hz > source_hz * 1.05;
+}
+
 static int present_vsync_owns_cadence(void) {
     if (g_video_vsync == 0 || g_present_vsync_disabled)
         return 0;
@@ -2924,7 +2955,7 @@ static int present_vsync_owns_cadence(void) {
         return 0;
     if (g_frame_period_ms <= 0.0)
         return 0;
-    if (g_frame_interpolation)
+    if (frame_interpolation_has_refresh_headroom())
         return 0;
     if (g_netplay_vsync_forced_off || psx_netplay_active())
         return 0;
@@ -2940,7 +2971,7 @@ extern "C" int psx_present_vsync_owns_cadence(void) {
 static int present_effective_swap_interval(void) {
     if (g_netplay_vsync_forced_off || psx_netplay_active())
         return 0;
-    if (g_frame_interpolation)
+    if (frame_interpolation_has_refresh_headroom())
         return 0;
     if (g_frame_period_ms <= 0.0)
         return 0;
@@ -2987,6 +3018,94 @@ static void apply_present_cadence(void) {
 #endif
 }
 
+#if defined(PSX_UWP)
+static RecompRuntimeUi *g_uwp_settings_ui;
+static std::array<uint32_t, 640 * 480> g_uwp_settings_pixels{};
+static constexpr const char *kUwpAaKey = "graphics.antialiasing";
+static constexpr const char *kUwpWideKey = "graphics.widescreen";
+
+static int uwp_settings_get(void *, const RecompRuntimeUiItem *setting, int *output_value) {
+    if (!setting || !output_value || !setting->key) return 0;
+    if (!std::strcmp(setting->key, RECOMP_RUNTIME_UI_KEY_RESOLUTION_SCALE))
+        *output_value = g_video_scale;
+    else if (!std::strcmp(setting->key, kUwpAaKey)) *output_value = g_video_aa ? 1 : 0;
+    else if (!std::strcmp(setting->key, kUwpWideKey))
+        *output_value = (g_video_aspect_num * 3 != g_video_aspect_den * 4) ? 1 : 0;
+    else return 0;
+    return 1;
+}
+
+static int uwp_settings_set(void *, const RecompRuntimeUiItem *setting, int new_value) {
+    if (!setting || !setting->key) return 0;
+    if (!std::strcmp(setting->key, RECOMP_RUNTIME_UI_KEY_RESOLUTION_SCALE)) {
+        g_video_scale = std::clamp(new_value, 1, 4);
+        gr_set_scale(g_video_scale);
+    } else if (!std::strcmp(setting->key, kUwpAaKey)) {
+        g_video_aa = new_value != 0;
+    } else if (!std::strcmp(setting->key, kUwpWideKey)) {
+        psx_mod_set_fixed_display_aspect(new_value ? 16u : 4u, new_value ? 9u : 3u);
+        gl_renderer_set_display_aspect(g_video_aspect_num, g_video_aspect_den);
+        vk_renderer_set_display_aspect(g_video_aspect_num, g_video_aspect_den);
+        if (sdl_renderer) {
+            g_logical_w = 480 * g_video_aspect_num * g_video_scale / g_video_aspect_den;
+            SDL_RenderSetLogicalSize(sdl_renderer, g_logical_w, 480 * g_video_scale);
+        }
+        g_ws_projection_mode = -1;
+        refresh_widescreen_projection();
+    } else return 0;
+    return 1;
+}
+
+static int uwp_settings_action(void *, const RecompRuntimeUiItem *setting) {
+    if (setting && setting->key && !std::strcmp(setting->key, RECOMP_RUNTIME_UI_KEY_RESUME)) {
+        recomp_runtime_ui_close(g_uwp_settings_ui);
+        return 1;
+    }
+    return 0;
+}
+
+static void uwp_settings_save(void *) {
+    psx_uwp_save_graphics_settings(
+        g_video_scale, g_video_aa ? 1 : 0,
+        (g_video_aspect_num * 3 != g_video_aspect_den * 4) ? 1 : 0);
+}
+
+static void uwp_settings_ensure(void) {
+    if (g_uwp_settings_ui) return;
+    static const RecompRuntimeUiItem settings_items[] = {
+        { RECOMP_RUNTIME_UI_KEY_RESOLUTION_SCALE, "Display", "Internal resolution", "Higher values sharpen 3D graphics; applies next launch.", RECOMP_RUNTIME_UI_INT, 1, 4, 1, nullptr, 0, nullptr },
+        { kUwpAaKey, "Display", "Image smoothing", "Smooth the final game image.", RECOMP_RUNTIME_UI_BOOL, 0, 1, 1, nullptr, 0, nullptr },
+        { kUwpWideKey, "Display", "16:9 widescreen", "Expand the rendered playfield for widescreen TVs.", RECOMP_RUNTIME_UI_BOOL, 0, 1, 1, nullptr, 0, nullptr },
+        { RECOMP_RUNTIME_UI_KEY_RESUME, "Display", "Resume game", "Return to Mega Man X5.", RECOMP_RUNTIME_UI_ACTION, 0, 0, 0, nullptr, 0, nullptr },
+    };
+    RecompRuntimeUiConfig config{};
+    config.title = "MEGA MAN X5 SETTINGS";
+    config.subtitle = "MENU + VIEW TO CLOSE";
+    config.items = settings_items;
+    config.item_count = sizeof(settings_items) / sizeof(settings_items[0]);
+    config.theme = "psx";
+    config.accept_label = "A SELECT";
+    config.back_label = "B CLOSE";
+    config.callbacks.get_value = uwp_settings_get;
+    config.callbacks.set_value = uwp_settings_set;
+    config.callbacks.run_action = uwp_settings_action;
+    config.callbacks.save = uwp_settings_save;
+    g_uwp_settings_ui = recomp_runtime_ui_create(&config);
+}
+
+extern "C" int psx_uwp_settings_overlay_image(const uint32_t **pixels,
+                                                int *width, int *height) {
+    if (!g_uwp_settings_ui || !recomp_runtime_ui_is_open(g_uwp_settings_ui)) return 0;
+    std::fill(g_uwp_settings_pixels.begin(), g_uwp_settings_pixels.end(), 0xff000000u);
+    recomp_runtime_ui_render_argb8888(g_uwp_settings_ui,
+        g_uwp_settings_pixels.data(), 640, 480, 640 * 4);
+    if (pixels) *pixels = g_uwp_settings_pixels.data();
+    if (width) *width = 640;
+    if (height) *height = 480;
+    return 1;
+}
+#endif
+
 static void log_present_cadence(void) {
     if (present_vsync_owns_cadence()) {
         /* The pacer still runs underneath as a deadline cap (see
@@ -6329,6 +6448,96 @@ static void rewind_pause_present(void) {
 #endif
 }
 
+#if defined(PSX_UWP)
+static int uwp_settings_combo_down(void) {
+    SDL_GameController *controller = g_players[0].handle;
+    return controller &&
+        SDL_GameControllerGetButton(controller, SDL_CONTROLLER_BUTTON_BACK) &&
+        SDL_GameControllerGetButton(controller, SDL_CONTROLLER_BUTTON_START);
+}
+
+static void uwp_settings_poll_toggle_buttons(void) {
+    static int was_down;
+    const int down = uwp_settings_combo_down();
+    if (down && !was_down && !savestate_menu_open && !psx_rewind_is_open()) {
+        uwp_settings_ensure();
+        if (g_uwp_settings_ui) {
+            recomp_runtime_ui_open(g_uwp_settings_ui);
+            recomp_runtime_ui_enter_section(g_uwp_settings_ui, 0);
+        }
+    }
+    was_down = down;
+}
+
+static void uwp_settings_host_pause_loop(void) {
+    int previous[5] = {};
+    uint32_t repeated_at[4] = {};
+    int combo_armed = 0;
+    freeze_heartbeat_set_paused(1);
+    gl_renderer_set_interpolation_suspended(1);
+    while (g_uwp_settings_ui && recomp_runtime_ui_is_open(g_uwp_settings_ui)) {
+        SDL_Event event;
+        while (SDL_PollEvent(&event)) {
+            if (event.type == SDL_QUIT) {
+                psx_crash_trace_set_exit_origin("sdl_window_close");
+                shutdown_runtime();
+                std::exit(0);
+            }
+        }
+        SDL_GameControllerUpdate();
+        SDL_GameController *controller = g_players[0].handle;
+        const int combo_down = uwp_settings_combo_down();
+        if (!combo_down) combo_armed = 1;
+        if (combo_down && combo_armed) {
+            recomp_runtime_ui_close(g_uwp_settings_ui);
+            break;
+        }
+
+        int current[5] = {};
+        if (controller) {
+            const Sint16 left_x = SDL_GameControllerGetAxis(controller, SDL_CONTROLLER_AXIS_LEFTX);
+            const Sint16 left_y = SDL_GameControllerGetAxis(controller, SDL_CONTROLLER_AXIS_LEFTY);
+            const int dead_zone = 16000;
+            current[0] = SDL_GameControllerGetButton(controller, SDL_CONTROLLER_BUTTON_DPAD_UP) ||
+                left_y < -dead_zone;
+            current[1] = SDL_GameControllerGetButton(controller, SDL_CONTROLLER_BUTTON_DPAD_DOWN) ||
+                left_y > dead_zone;
+            current[2] = SDL_GameControllerGetButton(controller, SDL_CONTROLLER_BUTTON_DPAD_LEFT) ||
+                left_x < -dead_zone;
+            current[3] = SDL_GameControllerGetButton(controller, SDL_CONTROLLER_BUTTON_DPAD_RIGHT) ||
+                left_x > dead_zone;
+            current[4] = SDL_GameControllerGetButton(controller, SDL_CONTROLLER_BUTTON_A);
+            if (SDL_GameControllerGetButton(controller, SDL_CONTROLLER_BUTTON_B)) {
+                recomp_runtime_ui_close(g_uwp_settings_ui);
+                break;
+            }
+        }
+        const uint32_t ticks = (uint32_t)SDL_GetTicks();
+        const RecompRuntimeUiInput inputs[5] = {
+            RECOMP_RUNTIME_UI_INPUT_UP, RECOMP_RUNTIME_UI_INPUT_DOWN,
+            RECOMP_RUNTIME_UI_INPUT_LEFT, RECOMP_RUNTIME_UI_INPUT_RIGHT,
+            RECOMP_RUNTIME_UI_INPUT_ACCEPT
+        };
+        for (int index = 0; index < 5; ++index) {
+            const int edge = current[index] && !previous[index];
+            const int repeat = index < 4 && current[index] && previous[index] &&
+                (int32_t)(ticks - repeated_at[index]) >= 170;
+            if (edge || repeat) {
+                recomp_runtime_ui_handle_input(g_uwp_settings_ui, inputs[index], 1, repeat);
+                repeated_at[index] = ticks;
+            }
+            previous[index] = current[index];
+        }
+        rewind_pause_present();
+        starvation_watchdog_heartbeat();
+        SDL_Delay(8);
+    }
+    gl_renderer_set_interpolation_suspended(0);
+    freeze_heartbeat_set_paused(0);
+    savestate_input_guard_arm();
+}
+#endif
+
 /* Freeze guest in vblank present while the rewind filmstrip is open. */
 static void rewind_host_pause_loop(void) {
     freeze_heartbeat_set_paused(1);
@@ -6705,12 +6914,19 @@ static NetplayVblankEpilogue sdl_vblank_present_body(void) {
         savestate_menu_poll_toggle_buttons();
         rewind_poll_toggle_buttons();
         fast_forward_toggle_poll_buttons();
+#if defined(PSX_UWP)
+        uwp_settings_poll_toggle_buttons();
+#endif
         psx_rewind_note_frame();
         psx_rewind_present_tick((uint32_t)SDL_GetTicks());
         if (savestate_menu_open)
             savestate_menu_host_pause_loop();
         if (psx_rewind_is_open())
             rewind_host_pause_loop();
+#if defined(PSX_UWP)
+        if (g_uwp_settings_ui && recomp_runtime_ui_is_open(g_uwp_settings_ui))
+            uwp_settings_host_pause_loop();
+#endif
     }
 
     /* Sample each player's device and feed the matching SIO pad slot.
diff --git a/runtime/src/psx_fiber.c b/runtime/src/psx_fiber.c
index a379eca..2a30d95 100644
--- a/runtime/src/psx_fiber.c
+++ b/runtime/src/psx_fiber.c
@@ -17,7 +17,7 @@ psx_fiber_t psx_fiber_convert_thread(void)
      * can return an unspecified TEB value that isn't the sentinel, causing
      * the sentinel check to silently skip ConvertThreadToFiber, leaving
      * the thread as a non-fiber, and crashing the first SwitchToFiber. */
-    void* fib = ConvertThreadToFiber(NULL);
+    void* fib = ConvertThreadToFiberEx(NULL, 0);
     if (!fib && GetLastError() == ERROR_ALREADY_FIBER)
         fib = GetCurrentFiber();
     return (psx_fiber_t)fib;
@@ -27,8 +27,8 @@ psx_fiber_t psx_fiber_current(void)      { return (psx_fiber_t)GetCurrentFiber()
 
 psx_fiber_t psx_fiber_create(size_t stack_size, psx_fiber_entry entry, void* arg)
 {
-    return (psx_fiber_t)CreateFiber((SIZE_T)stack_size,
-                                    (LPFIBER_START_ROUTINE)entry, arg);
+    return (psx_fiber_t)CreateFiberEx((SIZE_T)stack_size, (SIZE_T)stack_size, 0,
+                                      (LPFIBER_START_ROUTINE)entry, arg);
 }
 
 void psx_fiber_switch(psx_fiber_t target) { SwitchToFiber((LPVOID)target); }
diff --git a/tools/compile_overlays.py b/tools/compile_overlays.py
index 636d3d7..f510e16 100644
--- a/tools/compile_overlays.py
+++ b/tools/compile_overlays.py
@@ -7598,9 +7598,10 @@ def main():
     # that pass, using ABI-valid, current-byte guarded native coverage. Missing
     # even one primary root remains a failure; toolchain/audit failures above
     # are never cleared by this reconciliation.
-    reconcile_empty_primary_scans(
-        pending_empty_primary, cache_dir,
-        overlay_abi_tag(args.runtime_include, args.flavor), stats)
+    if not args.static:
+        reconcile_empty_primary_scans(
+            pending_empty_primary, cache_dir,
+            overlay_abi_tag(args.runtime_include, args.flavor), stats)
 
     # LOUD summary + machine-readable result line, then a non-zero exit when any
     # shard that should have built failed. The runtime's autocompile watcher and
